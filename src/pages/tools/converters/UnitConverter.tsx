// Backward-compatible route for existing links to /tools/converters/unit-converter.
// The converter directory is the canonical implementation and avoids duplicating its tool catalogue.
export { default } from "./index";
